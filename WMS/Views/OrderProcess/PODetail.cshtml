@{
    var grid = Html.DevExpress().GridView(settings =>
    {
        settings.Name = "PODetail_" + ViewData["POID"];
        settings.SettingsDetail.MasterGridName = "POView";
        settings.CallbackRouteValues = new { Controller = "RecevePO", Action = "PODetail", POID = ViewData["POID"] };
        settings.Width = System.Web.UI.WebControls.Unit.Percentage(100);

        settings.KeyFieldName = "PODetailID";

        //settings.SettingsPager.Visible = true;
        //	settings.Settings.ShowGroupPanel = true;
        //settings.Settings.ShowFilterRow = true;
        //settings.SettingsBehavior.AllowSelectByRowClick = true;

        //settings.Columns.Add("ReturnID");
        settings.Columns.Add("Warehouse");
        settings.Columns.Add("GTIN");
        settings.Columns.Add("Qty");
        settings.Columns.Add("Notes");
    });
}
@grid.Bind(Model).GetHtml()
